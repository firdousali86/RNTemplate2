import { kPathBrandItems, kPathNurseryItems } from '../config/WebService';
import { DataHelper } from '../helpers';
import { Colors } from '../theme';

class CommonUtils {}

export default new CommonUtils();
