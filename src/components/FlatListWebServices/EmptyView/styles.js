// @flow
import { StyleSheet } from "react-native";
import { Metrics } from "../../../theme/index";

export default StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
    marginTop: Metrics.doubleBaseMargin * 2.8
  }
});
